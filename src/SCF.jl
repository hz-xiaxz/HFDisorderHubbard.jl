"""
    getNMean(eigenstates::AbstractMatrix, i::Int)

calculate `⟨n_{i}⟩ = ∑_{α} |⟨i|α ⟩|^2` where i ∈ 1:2L, α ∈ 1:N. 1:L labels spin-up operators, L+1:2L labels spin-down operators.
"""
function getNMean(eigenstates::AbstractMatrix, i::Int)

end
